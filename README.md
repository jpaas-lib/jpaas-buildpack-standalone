## jpaas-buildpack-standalone

Buildpack for any `standalone app` which provides all runtimes and libraries by itself

### Usage
```bash
	jpaas push ${appname} -i 1 -m 512M -b https://github.com/jpaas-lib/jpaas-buildpack-standalone
```
